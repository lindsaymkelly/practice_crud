<h1> Current Posts: </h1>
<%= link_to 'Create New Post', new_post_path %>

<% @posts.each do |post| %>
  <div>
    <h2><%= post.title %></h2>
    <p><%= post.body %></p>
    <h3> Written by: <%= post.author.username %></h3>
  </div>
<% end %>